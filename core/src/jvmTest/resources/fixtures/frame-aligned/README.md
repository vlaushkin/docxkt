# Fixture: frame-aligned

**What this demonstrates:** second Phase 25 fixture. Paragraph
with alignment-based `<w:framePr>` (xAlign=left, yAlign=top)
+ drop-cap effect (drop, lines=3). hAnchor=TEXT, vAnchor=TEXT.

**Compared XML parts:** `word/document.xml`
