# Fixture: anchor-tight-wrap

**What this demonstrates:** second Phase 22 fixture. Floating
PNG with `<wp:wrapTight distT="0" distB="0"/>` — text wraps
closely around the bounding box (no `wrapPolygon` per
upstream's omission).

**Compared XML parts:** `word/document.xml`
**Compared binary parts:** `word/media/image1.png`
