# Fixture: toc-default

**What this demonstrates:** first Phase 18 fixture. Minimal
table-of-contents block: alias "Contents", hyperlink flag set,
heading style range 1-3.

- `word/document.xml` — `<w:sdt>` wrapping `<w:sdtPr>` with
  alias and `<w:sdtContent>` with two paragraphs: begin/
  instrText/separate in the first, end in the second.

**Compared XML parts:**
- `word/document.xml`
