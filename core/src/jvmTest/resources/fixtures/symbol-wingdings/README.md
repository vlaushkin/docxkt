# Fixture: symbol-wingdings

**What this demonstrates:** first Phase 21 fixture. A single
`<w:sym>` glyph from the Wingdings font — the `F0FC` check mark.

**Compared XML parts:**
- `word/document.xml`
