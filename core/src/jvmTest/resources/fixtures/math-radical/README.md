# Fixture: math-radical

**What this demonstrates:** first Phase 26 fixture. Two
math paragraphs — square root `√(x + 1)` and cube root
`∛y`. Asserts the `<m:radPr>` / `<m:degHide>` / `<m:deg>`
switch upstream uses to differentiate square vs n-th
roots.

**Compared XML parts:** `word/document.xml`
