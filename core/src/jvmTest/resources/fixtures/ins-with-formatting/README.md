# Fixture: ins-with-formatting

**What this demonstrates:** third Phase 20 fixture. An
inserted run carries bold rPr inside its inner `<w:r>` —
asserts the wrapper doesn't strip run-level formatting.

**Compared XML parts:**
- `word/document.xml`
