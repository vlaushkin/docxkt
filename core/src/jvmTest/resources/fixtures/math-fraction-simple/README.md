# Fixture: math-fraction-simple

**What this demonstrates:** Phase 26 fraction fixture.
Plain `a/b` fraction. Asserts no `<m:fPr>` is emitted
(upstream omits when defaults).

**Compared XML parts:** `word/document.xml`
