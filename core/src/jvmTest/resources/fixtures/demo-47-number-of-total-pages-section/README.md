# Fixture: demo-47-number-of-total-pages-section

Phase 41 port of `/opt/docx-ref/demo/47-number-of-total-pages-section.ts`.

Two sections, each with default header + footer (rId7-10) and
`pageNumbers(start = 1, format = DECIMAL)`. Body has page-break
paragraphs in each section.

**Compared XML parts:** `word/document.xml`
