# Fixture: demo-70-line-numbers-suppression

Phase 37 port of `/opt/docx-ref/demo/70-line-numbers-suppression.ts`.

Section with line numbers + a paragraph with
`suppressLineNumbers = true` (mid-document suppression).

**Compared XML parts:** `word/document.xml`
