# Fixture: sdt-checkbox-checked

**What this demonstrates:** first Phase 24 fixture. Checked
checkbox with alias "Accept", default symbols
(2612 / 2610 in MS Gothic). Asserts `<w14:checked w14:val="1"/>`
and the rendered `<w:sym>` matches the checked state symbol.

**Compared XML parts:** `word/document.xml`
