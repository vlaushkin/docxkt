# Fixture: demo-43-images-to-table-cell-2

Phase 53 port. 4×4 table with two cells holding images
(small base64 PNG, then a real JPEG); third row has a "Hello"
text cell; rows 1,2,4 have empty cells; row 2 last cell has
rowSpan=2 → continued in row 3.

Hash media filenames normalized to image1.png / image2.jpg
in the order they appear in the upstream rels file.

**Compared XML parts:** auto-discovered.
