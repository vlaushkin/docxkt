# Fixture: field-author

**What this demonstrates:** fourth Phase 15 fixture. A
`<w:fldSimple>` wrapping a single `<w:r><w:t>` cached-display
run. Covers the simple-with-body wire, proving we emit the
text run inside the field wrapper (not as a sibling).

**Compared XML parts:**
- `word/document.xml`
