# Fixture: math-function-sin

**What this demonstrates:** Phase 27 function fixture.
`<m:func>` with `<m:funcPr/>`, `<m:fName>sin</m:fName>` and
`<m:e>x</m:e>`. Renders as `sin(x)`.

**Compared XML parts:** `word/document.xml`
