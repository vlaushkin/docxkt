# Fixture: math-subscript

**What this demonstrates:** Phase 27 subscript fixture.
`<m:sSub>` with `<m:sSubPr/>`, `<m:e>` (base) and
`<m:sub>` (subscript). Renders as `xᵢ`.

**Compared XML parts:** `word/document.xml`
