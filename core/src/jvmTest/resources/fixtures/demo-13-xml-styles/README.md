# Fixture: demo-13-xml-styles

Phase 37 port of `/opt/docx-ref/demo/13-xml-styles.ts`.

Custom external styles (Heading1 + MyFancyStyle). We compare
only `word/document.xml` (upstream's externalStyles file is
loaded separately; we don't recreate it).

**Compared XML parts:** `word/document.xml`
