# Fixture: ins-run

**What this demonstrates:** first Phase 20 fixture. One
`<w:ins>` wrapping a plain-text run inside a paragraph
with preceding context text.

**Compared XML parts:**
- `word/document.xml`
