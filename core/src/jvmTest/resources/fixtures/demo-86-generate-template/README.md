# Fixture: demo-86-generate-template

Phase 37b port of `/opt/docx-ref/demo/86-generate-template.ts`.

A trivial template-source document — one paragraph holding the
literal token `{{template}}`. Useful as a baseline.

**Compared XML parts:** `word/document.xml`
