# Fixture: field-numpages

**What this demonstrates:** third Phase 15 fixture. Complex-form
`NUMPAGES` field — same wire shape as field-page but with a
different instruction string. Exercises the same
begin/instrText/separate/end chain inside a single run.

**Compared XML parts:**
- `word/document.xml`
