# Fixture: math-nary-integral

**What this demonstrates:** Phase 27 integral fixture.
Definite integral from `0` to `1` of `f(x) dx`. No
`<m:chr>` (Word renders default integral glyph), `limLoc =
subSup`.

**Compared XML parts:** `word/document.xml`
