# Fixture: demo-36-image-to-table-cell

Phase 53 port. The same image-in-cell table emitted in both the
default header and the body. Body image rId on document.xml.rels;
header image rId on header1.xml.rels (per-part rels). One physical
media binary shared by content hash.

Hash filename normalized to image1.jpg.

**Compared XML parts:** auto-discovered.
