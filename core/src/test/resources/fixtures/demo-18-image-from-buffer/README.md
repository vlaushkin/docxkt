# Fixture: demo-18-image-from-buffer

Phase 44 port of `/opt/docx-ref/demo/18-image-from-buffer.ts`.

Single section, single paragraph with one inline 100×100 image
decoded from a base64 PNG buffer. Image is preserved as
`buffer.png` next to the README so the test class can decode it
at run time.

**Compared XML parts:** auto-discovered (Phase 42 default).
