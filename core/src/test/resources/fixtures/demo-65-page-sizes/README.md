# Fixture: demo-65-page-sizes

Phase 37 port of `/opt/docx-ref/demo/65-page-sizes.ts`.

Two sections with explicit non-A4 page sizes (A5 landscape +
A3 portrait). Twip values inlined since we don't have a
convertMillimetersToTwip helper.

**Compared XML parts:** `word/document.xml`
