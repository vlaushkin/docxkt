# Fixture: footer-page-of-total

**What this demonstrates:** second Phase 16 fixture. Footer
reading "Page N of M" — exercises both `pageNumber()` and
`totalPages()` DSL wrappers in a single paragraph.

**Compared XML parts:**
- `word/footer1.xml`
