# Fixture: demo-62-paragraph-spacing

Phase 37 port of `/opt/docx-ref/demo/62-paragraph-spacing.ts`.

Two paragraphs: first with before/after spacing 5000 each,
second with line=2000 + lineRule=auto.

**Compared XML parts:** `word/document.xml`
