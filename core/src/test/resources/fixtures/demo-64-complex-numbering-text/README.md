# Fixture: demo-64-complex-numbering-text

Phase 47 port of `/opt/docx-ref/demo/64-complex-numbering-text.ts`.

ref1 with 3 levels using complex level text patterns
(`%1`, `%1.%2`, `%1.%2.%3`). Body uses both default and explicit
instance ids.

**Compared XML parts:** auto-discovered (Phase 42 default).
