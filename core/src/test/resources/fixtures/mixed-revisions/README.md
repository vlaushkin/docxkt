# Fixture: mixed-revisions

**What this demonstrates:** Phase 20 stretch fixture. One
paragraph combines plain text + `<w:ins>` + plain text +
`<w:del>` + plain text. Asserts revision-id auto-allocation
(or explicit per-call id) plays nicely with interleaved
regular runs.

**Compared XML parts:**
- `word/document.xml`
