# Fixture: demo-67-column-break

Phase 43 port of `/opt/docx-ref/demo/67-column-break.ts`.

Single section with 2-column layout (count=2, space=708) and a
single paragraph holding two text runs separated by a column
break.

**Compared XML parts:** auto-discovered (Phase 42 default).
