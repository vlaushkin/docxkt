# Fixture: demo-42-restart-page-numbers

Phase 41 port of `/opt/docx-ref/demo/42-restart-page-numbers.ts`.

Two sections: each has default + first headers (rId7-10). Section
2 carries `pageNumbers(start = 1, separator = EM_DASH)`. Body has
two paragraphs per section with page break.

**Compared XML parts:** `word/document.xml`
