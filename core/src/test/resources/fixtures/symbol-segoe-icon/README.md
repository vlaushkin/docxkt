# Fixture: symbol-segoe-icon

**What this demonstrates:** second Phase 21 fixture. Symbol
from "Segoe UI Symbol" font (char `2713`) — asserts the
`w:font` attribute round-trips a multi-word font name with
spaces intact in the attribute value.

**Compared XML parts:**
- `word/document.xml`
