# Fixture: demo-90-check-boxes

Phase 45 port of `/opt/docx-ref/demo/90-check-boxes.ts`.

Single section, single paragraph with mixed text and check-box
SDTs: default unchecked, default checked, custom symbol "2611",
custom font "MS Gothic", custom checked + unchecked, alias.

**Compared XML parts:** auto-discovered (Phase 42 default).
