# Fixture: footnote-single

**What this demonstrates:** first Phase 17 fixture. One user
footnote referenced from one body paragraph. Exercises the
default separator + continuationSeparator auto-emission, the
body-side `FootnoteReferenceRun` with character-style rPr, and
the auto-prepended `footnoteRef` marker in the footnote's first
paragraph.

**Compared XML parts:**
- `word/document.xml`
- `word/footnotes.xml`
