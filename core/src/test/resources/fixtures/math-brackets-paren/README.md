# Fixture: math-brackets-paren

**What this demonstrates:** Phase 26 round-brackets
fixture. `<m:d>` with empty `<m:dPr/>` — Word renders
default round parentheses around the inner math.

**Compared XML parts:** `word/document.xml`
