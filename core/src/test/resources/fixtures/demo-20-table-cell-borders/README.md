# Fixture: demo-20-table-cell-borders

Phase 37b port of `/opt/docx-ref/demo/20-table-cell-borders.ts`.

A 4×4 table where one inner cell carries custom `<w:tcBorders>`
on all four sides (mixed styles + colors). Other cells are empty.

**Compared XML parts:** `word/document.xml`
