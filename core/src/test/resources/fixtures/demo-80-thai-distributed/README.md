# Fixture: demo-80-thai-distributed

Phase 37b port of `/opt/docx-ref/demo/80-thai-distributed.ts`.

A single Thai paragraph at size=28 with `thaiDistribute`
alignment, inside a section with custom margins (top=0,
right/bottom/left = 1360 twips ≈ 24mm).

**Compared XML parts:** `word/document.xml`
