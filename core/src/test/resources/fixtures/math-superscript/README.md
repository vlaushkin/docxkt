# Fixture: math-superscript

**What this demonstrates:** Phase 27 superscript fixture.
`<m:sSup>` with `<m:sSupPr/>` (empty), `<m:e>` (base) and
`<m:sup>` (exponent). Renders as `x²`.

**Compared XML parts:** `word/document.xml`
