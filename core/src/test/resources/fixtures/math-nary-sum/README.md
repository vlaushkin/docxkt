# Fixture: math-nary-sum

**What this demonstrates:** Phase 27 sum fixture. Summation
∑ from `i=1` to `n` of `x`. Asserts `<m:nary>` with
`<m:chr m:val="∑"/>` and `<m:limLoc m:val="undOvr"/>` in
properties.

**Compared XML parts:** `word/document.xml`
