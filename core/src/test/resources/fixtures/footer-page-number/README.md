# Fixture: footer-page-number

**What this demonstrates:** first Phase 16 fixture. Footer
containing a centered paragraph that reads `"Page N"` where
`N` is a complex-form PAGE field. Exercises the
`footer { paragraph { pageNumber() } }` path.

- `word/footer1.xml` — a centered paragraph with `"Page "`
  and a complex-field run emitting the PAGE instruction.

**Compared XML parts:**
- `word/footer1.xml`
