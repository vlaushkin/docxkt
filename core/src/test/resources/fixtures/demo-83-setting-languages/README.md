# Fixture: demo-83-setting-languages

Phase 37b port of `/opt/docx-ref/demo/83-setting-languages.ts`.

Three paragraphs in Spanish/French/Korean. The 2nd and 3rd
reference paragraph styles (`frenchNormal`, `koreanNormal`); the
language attribute lives in styles.xml (not compared). Exercises
UTF-8 + apostrophe escaping (`&apos;`).

**Compared XML parts:** `word/document.xml`
