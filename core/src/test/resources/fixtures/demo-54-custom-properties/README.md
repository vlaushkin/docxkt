# Fixture: demo-54-custom-properties

Phase 43 port of `/opt/docx-ref/demo/54-custom-properties.ts`.

Empty document with core properties (creator/title/subject/description)
plus two custom properties (Subtitle, Address). Body is empty.

**Compared XML parts:** auto-discovered (Phase 42 default).
