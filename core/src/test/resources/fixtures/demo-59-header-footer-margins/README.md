# Fixture: demo-59-header-footer-margins

Phase 41 port of `/opt/docx-ref/demo/59-header-footer-margins.ts`.

Single section with default header + footer (rId7/rId8) and
custom header/footer margin offsets (`pgMar` `header=100 footer=50`).

**Compared XML parts:** `word/document.xml`
