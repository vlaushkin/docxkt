# Fixture: demo-84-positional-tabs

Phase 49a port. Two paragraphs with `<w:ptab>` (positional tab)
inside formatted runs. RIGHT/MARGIN/DOT and CENTER/INDENT/HYPHEN.

**Compared XML parts:** auto-discovered.
