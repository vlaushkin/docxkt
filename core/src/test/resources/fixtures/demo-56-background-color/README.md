# Fixture: demo-56-background-color

Phase 39 port of `/opt/docx-ref/demo/56-background-color.ts`.

Sets a document-level background color (`<w:background w:color="C45911"/>`)
and contains the same three-run paragraph as demo-1 (with leading
tab inside a formatted run). Closes Phase 37a "document-level
background color" gap.

**Compared XML parts:** `word/document.xml`
