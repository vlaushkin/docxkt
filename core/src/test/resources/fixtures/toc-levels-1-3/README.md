# Fixture: toc-levels-1-3

**What this demonstrates:** second Phase 18 fixture. TOC with
`headingStyleRange = "1-3"` but no hyperlink — asserts the
`\h` switch is absent from the instruction string.

**Compared XML parts:**
- `word/document.xml`
