# Fixture: del-run

**What this demonstrates:** second Phase 20 fixture. One
`<w:del>` wrapping a deleted text run — inner text uses
`<w:delText>` instead of `<w:t>`.

**Compared XML parts:**
- `word/document.xml`
