# Fixture: sdt-checkbox-unchecked

**What this demonstrates:** second Phase 24 fixture.
Unchecked checkbox without alias. Asserts the `<w:alias>`
element is omitted entirely (not emitted with empty val) and
the rendered `<w:sym>` uses the unchecked state's `2610`
character.

**Compared XML parts:** `word/document.xml`
