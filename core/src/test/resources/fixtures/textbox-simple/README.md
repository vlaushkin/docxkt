# Fixture: textbox-simple

**What this demonstrates:** first Phase 23 fixture. Inline
DrawingML textbox (`<wps:wsp>` inside `<a:graphicData
uri="…wordprocessingShape">`) with a single paragraph
"Inside textbox". Empty `<wps:bodyPr/>`.

**Compared XML parts:** `word/document.xml`
